import { createTheme } from "@mui/material";

export const theme = createTheme({
  shadows: 0,
  typography: {
    fontFamily: 'Poppins, sans-serif'
  },
  palette: {
    background: {
      default:'#161E35'
    },
    light: '#FFFFFF',
    grey: '#ACABBD',
    erro: '#F8032D'
  },
});
